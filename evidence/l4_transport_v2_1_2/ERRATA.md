# Metadata erratum

The raw `summary.json` claim boundary says “across three finite-response level sets”. The frozen configuration contains one tested response shift, `0.08`; operationally the experiment uses a source level set and one target level set.

The raw evidence is preserved unchanged for auditability. Repository prose should say:

> Local transport between two distinct finite-response level sets in one GPT-2 LoRA chart.

This correction changes descriptive metadata only; it does not change metrics, gates, configuration, or raw records.
