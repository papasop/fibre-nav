# Fibre-nav v1.8.1 GitHub overlay

Copy the contents of this package into the repository root. It adds one frozen
development snapshot at:

`evidence/l3_category_v1_8_1/`

Suggested commit message:

`Archive MFI L3 shared-category writer v1.8.1 partial result`

Do not tag this snapshot as a confirmed L3 release. The single frozen seed is a
partial mechanism result and is retained to make the transition from v1.8.0 to
v1.8.2 auditable.

The package does not delete or replace existing repository files.
