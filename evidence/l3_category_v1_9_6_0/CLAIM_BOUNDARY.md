# Claim boundary

## Supported by this archive

- The fresh-split 80-concept router passed every declared routing and control gate.
- Four frozen bridge instances were routed correctly with positive margins to a fixed category-address map.
- Sixteen rank-8 writer endpoints passed exact, training-expression, held-out-expression, mixed-address, response-budget, and endpoint-KL gates.
- Readout remained correct across four cyclic concept–address assignments.
- The integrated pipeline passed every declared gate for frozen seed `82601`.
- This is one prospectively frozen single-seed confirmation of the tested L3 mechanism.

## Not supported

- Multi-seed confirmation or population-level statistical stability;
- an entirely new external concept cohort;
- arbitrary linguistic equivalence beyond the declared held-out expressions;
- arbitrary address permutations beyond the tested cyclic family;
- global response invariance over all possible inputs;
- a smooth global fibre bundle;
- cross-model generality;
- structure-preserving cross-fibre transport (L4);
- programmable fibre computation (L5);
- replacement of distributed neural representations.

