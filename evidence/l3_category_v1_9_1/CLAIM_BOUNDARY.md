# Claim boundary

## Supported in development

- Exact memory anchoring, reading and overwrite under declared budgets.
- Held-out linguistic-expression access for previously written memories.
- Reusable two-slot compositional addresses under tested cyclic reassignments.
- A fully gated L3 category-geometry result on one adaptive-development seed.

## Pending

- Independent replication on fresh seed `82101` and the frozen new split.
- Multi-seed confirmation.
- Cross-model replication.
- L4 cross-fibre, path-independent or structure-preserving transport.

The words `confirmed`, `universal`, `natural intrinsic layer`, and
`transportable concept geometry` should not describe this snapshot.
