# V8 工程验证范围

20项工程测试通过：64个输入冻结且映射到16个V6标准提示；完整128点计划；构造提示与运行时处理不调用真值；三种文本格式有/无规则前缀；24负例经真实process_input拒绝且后端调用为0；重复键与非法类型、超长与深度嵌套输入拒绝；后端只接收编译提示；字段/编译错误不能由正确答案掩盖；模型错误不被语法纠正；有效输入误拒绝被记录为失败；对齐logit审计与旧指标保留；掩码/策略/token/缺失数据拒绝；环境、重复、未完成门槛；每点真实走解析/编译后调用测试后端，阶段归档与完成后不重复；17点中断续跑；启动器源/续跑ZIP校验、旧协议拒绝及独立进程argv。

最终ZIP经真实启动器检查解包，独立python -I -S运行--check-only，不加载模型；对解包内容重跑工程测试。
随机小Qwen3的CPU检查覆盖实际受限生成、掩码、强制EOS、候选评分及两个末位置诊断前向，并确认受限调用不污染自由调用。此处自由调用只是工程检查，V8正式实验不测自由生成。

本地小模型环境torch2.6.0+cpu、transformers4.57.1；目标实验固定torch2.8.0+cpu、transformers4.56.2。不能把本地检查当作目标版本下的预训练Qwen结果。
没有运行预训练Qwen V8，没有训练、参数记忆写入、Hugging Face上传或付费任务。
V7完整结果ZIP仍未审计，不根据日志声称其主分数或数值路径已经复核。
