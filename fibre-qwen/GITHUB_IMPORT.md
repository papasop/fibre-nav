# Import instructions

Copy this directory into the target repository as `fibre-qwen/`. Preserve existing repository files outside that path. Review `MANIFEST.sha256`, run Python syntax checks for protocol scripts, then update the repository-level README and provenance index before committing.

This is a development overlay, not a trained model release. R21b-r1 remains fail-closed and R21b-r2 has no result yet.
