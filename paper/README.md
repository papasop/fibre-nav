# Manuscript snapshot

`Moving_Response_Fibres_v5_unified_hierarchy.pdf` is the reviewed manuscript
snapshot accompanying this evidence bundle.

It correctly separates the 24-node low-response Pareto protocol from the
32-node GPTW-v3 correction-cost protocol. Before a final paper release, update
the visible subtitle and PDF metadata from the old
`Realizability-Cost Scaling and Transverse Response Amplification` wording to
the selected two-part hierarchy, preferably:

`Geometry and Low-Response Learning`.

The Evidence ledger table should also be pinned before the Discussion heading
in the LaTeX source (`[H]` or `\FloatBarrier`) so it cannot float into Section
10. The PDF is included as an auditable snapshot, not represented as the final
typeset release.
