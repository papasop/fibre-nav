# Compressed revision notes

Source basis: manuscript supplied as `20260907-065746`.

The compressed version preserves the L1-to-L5 mechanism chain and the L5 shared-state climax while making the following editorial changes:

- replaces the full protocol-provenance table with a concise provenance paragraph;
- compresses the L4 alternative-explanation discussion;
- consolidates twelve limitation items into seven non-overlapping boundaries;
- replaces inline SHA-256 blocks with the audited repository commit and manifest locations;
- preserves all principal L3, L4 and L5 quantitative results and statistical qualifications;
- preserves the distinction between independent protocol records and a staged mechanism argument.

Validation: the revised source compiles with `pdflatex` to eight pages with no LaTeX warnings or overfull boxes.
